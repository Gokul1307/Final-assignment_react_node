

import Form from "./form";


describe("Dashboard", () => {
    it("should render my component", () => {
      // eslint-disable-next-line no-unused-vars
      const Dashboard = expect(<Form />);
    });
  });