import Dashboard from '../Dashboard/Dashboard'


describe("login", () => {
    it("should render my component", () => {
      // eslint-disable-next-line no-unused-vars
      const login = expect(<Dashboard />);
    });
  });